@using GameManage.Logic.Models
@model GameManagementTool.Models.ShowAllCharactersViewModel
<link href="@Url.Content("~/css/CharacterPage.css")" rel="stylesheet" type="text/css"/>
<script src="/../wwwroot/js/site.js" language="javascript" type="text/javascript"></script>

@{
    ViewData["Title"] = "Index";
}

<h2>Character Page</h2>

<p>
    <a class="btn btn-danger" role="button" aria-pressed="true" asp-action="AddNewCharacter">CREATE NEW</a>
</p>
<input type="text" id="myInput" onkeyup="SearchCharacterName()" placeholder="Search for your character.." title="Type in a character name">
<table id="adminTable" class="table table-hover table-striped">
    <thead class="thead-dark">
        <tr>
            <th>Name</th>
            <th>Specializaton</th>
            <th>Score</th>
            <th>Created On</th>
            <th>Edit</th>
        </tr>
    </thead>
    <tbody>
    @foreach (Character character in Model.Characters)
    {
        <tr>
            <td>@character.Name</td>
            <td>@character.SpecializationName</td>
            <td>@character.Score</td>
            <td>@character.CreatedOn</td>
            <td><a asp-action="Edit" asp-controller="Character" asp-route-id="@character.Id">EDIT</a></td>
        </tr>
    }
    </tbody>
</table>

